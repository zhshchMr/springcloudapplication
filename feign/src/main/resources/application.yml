server:
  port: 8050
spring:
  application:
    name: feign
eureka:
  client:
    service-url:
      defaultzone: http://localhost:8761/eureka/
    instance:
      prefer-ip-adress: true
feign:
  hystrix:
    enabled: true
